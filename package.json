{
  "name": "zammad-mcp",
  "version": "0.1.0",
  "description": "MCP Server for Zammad: automated shared drafts with strict reply-HTML validation, fresh signature rendering and German-localised quote blocks.",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "zammad-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "node --test tests/*.test.ts"
  },
  "keywords": ["mcp", "zammad", "ticketing", "shared-draft", "email"],
  "author": "Phillip Baumgärtner",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
